
const reducer = (state=[],action)=> {

        switch(action.type) {
            // case "ADD_CONTACT":
            // state=[...state,action.payload]
            // default:
            default:
                return state
                
        }
}

export default reducer


